# 2026-06-13

- 修复 PyMySQL TINYINT(1)→bool 导致 DictCursor 列映射混乱的重大 bug：在 database/db_manager.py 中新增 SafeDictCursor 类，重写 _do_get_result 用 cursor.description 重建行字典并强制 bool→int。已在 execute_query、execute_one、transaction 三处替换 DictCursor 为 SafeDictCursor。
